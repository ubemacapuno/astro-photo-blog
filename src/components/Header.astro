---
import NavMenu from "./vue/NavMenu.vue";
import ThemeIcon from "./ThemeIcon.astro";

const pathname = Astro.url.pathname;
const isHomepage = pathname === "/";
---

<header>
  <nav class="relative">
    <div class="flex justify-between items-center">
      <div class="md:hidden flex items-center">
        {
          !isHomepage && (
            <a
              href="/"
              data-astro-prefetch
              class="text-3xl tracking-wide uppercase anton-regular no-underline text-red-600"
            >
              Corey Damocles
            </a>
          )
        }
      </div>
      <NavMenu client:load pathname={pathname} />
      <div class="hidden md:block md:ml-auto">
        <ThemeIcon />
      </div>
    </div>
  </nav>
</header>
